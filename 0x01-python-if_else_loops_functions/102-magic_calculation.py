#!/usr/bin/python3
'''the Python function def magic_calculation(a, b, c): that takes three arguments and does exactly the same as python bytecode:'''
def magic_calculation(a, b, c):
    if a < b:
        return (c)
    if c > b:
        return (a + b)
    return (a*b - c)
